using QuestPDF.Helpers;

namespace QuestPDFOverlayIssue
{
    internal static class Layout
    {
        internal static readonly PageSize PAGE_SIZE = PageSizes.Letter;
        internal const float BLEED = 0.125f;
        internal const float MARGIN = 0.5f;
    }
}
